meta:
  environment: node
  toolchain:
    name: npm
    version: 18
build:
  additionalCommands: npm install
  scriptArguments: npm install
run:
  scriptName: /src/main.js
  persistenceMount: /data
  containerPort: 80
